# Write your MySQL query statement below
SELECT person_name
FROM (
    SELECT person_name,
    SUM(weight) OVER (ORDER BY turn asc) AS total_weight
    FROM Queue
)t
WHERE total_weight<=1000
ORDER BY total_weight DESC  #put last person boarded on top
LIMIT 1;  #to get the top one 
