# Write your MySQL query statement below
SELECT r.contest_id,
ROUND(COUNT( r.user_id)*100/(SELECT COUNT(*) FROM USERS u),2) AS percentage
FROM Register r
#JOIN Register r ON u.user_id=r.user_id      there was no need of using join here
GROUP BY r.contest_id 
ORDER BY percentage DESC ,r.contest_id ASC
