#learnt how to use CASE WHEN FOR different condition like in c ,java
# Write your MySQL query statement below
SELECT 
CASE
WHEN id%2=1 AND id=(SELECT MAX(id) FROM SEAT)
then id
WHEN id%2=1
then id+1
ELSE id-1
END AS
id,student
FROM Seat
ORDER BY id;
